<h1>Book index work in progress</h1>
<p>index area</p>

<% @books.each do |book| %>
<table>
 <thead>
     <tr>
      <th>Title</th>
      <th>Body</th>
      <th colspan="3"></th>
     </tr>    
 </thead>
 
 <tbody>
<td><%= book.title %></td>
<td><%= book.body %></td>
<td><%= link_to "Show", "/books/174420" %></td>
<td><%= link_to "Edit", "/books/174427/edit" %></td>
<td><p>delete</p></td>
</tbody>
</table>
<% end %>






<!--以下投稿フォーム-->

<%= form_with model: @book, url: '/books', method: :post do |f| %>


<h4>Title</h4>
<%= f.text_field :title %>

<h4>Body</h4>
<%= f.text_area :body %>

<%= f.submit '投稿' %>

<% end %>



<%= link_to "Show", "/books/174420" %>
<%= link_to "Edit", "/books/174427/edit" %>